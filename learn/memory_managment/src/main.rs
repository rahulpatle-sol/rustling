fn main() {

    let x = 42; // i32 — stored on stack
let arr = [1, 2, 3]; // fixed-size array — stack

let s = String::from("Rahul"); // heap-allocated string
let v = vec![1, 2, 3];         // heap-allocated vector


}
